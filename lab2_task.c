#include<stdio.h>

int main()
{
    int x,y,sum, mul, div, sub;
    x=10; //this is assignment operator.
    y=20;
    sum= x+y;
    mul= x*y;
    div= y/x;
    sub= x-y;
char an;
an= 'T';
printf("The sum is: %d\n", sum);/*this is printf function. here it's calculating and printing Sum */
        printf("The multiplication is: %d\n", mul);
            printf("The division is: %d\n", div);
                printf("The subtraction is: %d\n", sub);
                    printf("The character is: %c", an);
double A, B, Sum, Mul, Sub;
float Div;
 A=250; //this is assignment operator.
    B=20;
    Sum= A+B;
    Mul= A*B;
    Div= A/B;
    Sub= A-B;

    printf("The sum is: %lf\n", Sum);/*this is printf function. here it's calculating and printing Sum */
        printf("The multiplication is: %lf\n", Mul);
            printf("The division is: %f\n", Div);
                printf("The substraction is: %lf\n", Sub);

    return 0;
}
